{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalEnv": ["DATABASE_URL", "AUTH_SECRET", "AUTH_TRUST_HOST", "AUTH_URL", "APP_URL", "NEXT_PUBLIC_APP_URL", "NEXTAUTH_URL", "PII_ENCRYPTION_KEY", "PII_HASH_PEPPER", "CRON_SECRET", "MOYASAR_API_KEY", "MOYASAR_WEBHOOK_SECRET", "TURNSTILE_SECRET_KEY", "NEXT_PUBLIC_TURNSTILE_SITE_KEY", "NODE_ENV", "ZATCA_MASTER_KEY", "ZATCA_ENVIRONMENT"],
  "globalPassThroughEnv": ["CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["DATABASE_URL", "AUTH_SECRET", "AUTH_TRUST_HOST", "AUTH_URL", "APP_URL", "NEXT_PUBLIC_APP_URL", "NEXTAUTH_URL", "NEXT_PUBLIC_API_URL"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types", "^db:generate"]
    },
    "test:unit": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "db:generate": {
      "cache": false
    }
  }
}
