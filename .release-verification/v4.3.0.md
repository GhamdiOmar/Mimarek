# Release Verification — v4.3.0

Date: 2026-05-16
Verified by: Claude Code (pre-merge, on branch `claude/tender-sammet-4098bd` synced to `origin/main` @ v4.2.5)
Server: localhost:3100 (`next start`, production build) — port 3000 held by a parallel worktree
Harness: Playwright + Chromium (preview-renderer screenshots time out in this env; Playwright used instead)

---

## Routes Verified

1. /dashboard/marketplace (buyer browse)
2. /dashboard/marketplace/[listingId] (listing detail)
3. /dashboard/marketplace/my-listings (seller listings + incoming inquiries)
4. /dashboard/admin/marketplace (platform moderation — system user)
5. /dashboard/units (Publish-in-Marketplace entry point)
6. /dashboard/crm (Marketplace source badge/filter)

---

## Screenshots (17 — `apps/web/e2e/__screenshots__/marketplace/`)

Combos captured: light-LTR, light-RTL, dark-LTR, dark-RTL.

### /dashboard/marketplace (browse)
- browse.light.ltr.png ✓
- browse.light.rtl.png ✓
- browse.dark.ltr.png ✓
- browse.dark.rtl.png ✓
- browse.mobile.rtl.png ✓ (375×812)

### /dashboard/marketplace/[listingId] (detail)
- detail.light.ltr.png ✓
- detail.light.rtl.png ✓
- detail.dark.ltr.png ✓
- detail.dark.rtl.png ✓

### /dashboard/marketplace/my-listings
- my-listings.light.ltr.png ✓
- my-listings.light.rtl.png ✓
- my-listings.dark.ltr.png ✓
- my-listings.dark.rtl.png ✓

### /dashboard/admin/marketplace (platform moderation)
- admin-moderation.light.ltr.png ✓
- admin-moderation.dark.rtl.png ✓

### My Listings discoverability fix
- browse-with-mylistings-button.png ✓ (header "My Listings" button visible)
- mylistings-with-back-link.png ✓ ("Back to marketplace" link visible)

---

## Functional Cross-Org E2E (Playwright, two real orgs)

Seller `admin@mimaric.sa` (Mimaric org) · Buyer `dummy@demo.sa` (Dummy org) · Platform `system@mimaric.sa`.

| Step | Result |
|---|---|
| Seller publish (units → eligibility gate → publish dialog) | ✓ `MISSING_ADDRESS` blocker enforced, then publishes |
| Buyer cross-org browse sees seller listing | ✓ (hardened read layer) |
| Listing detail — Google Maps `?api=1` URL exact, short address | ✓ |
| Buyer Express Interest (confirmation modal) | ✓ inquiry created |
| Seller-side CRM customer auto-created | ✓ `source=MARKETPLACE`, in seller org (DB-asserted) |
| Convert inquiry → deal | ✓ cross-org reservation + `PENDING_SETTLEMENT` transfer (DB-asserted) |
| Settlement without SIGNED sale contract | ✓ **correctly refused** (spec's primary risk control) |
| Buyer-browse own-org exclusion (seller sees no own listing) | ✓ `toHaveCount(0)` |
| My Listings reachable via header button → route → back link | ✓ (regression spec) |

Spec: `apps/web/e2e/marketplace.cross-org.spec.ts`, `apps/web/e2e/marketplace.mylistings-link.spec.ts` (config `playwright.marketplace.config.ts`). Both green.

---

## Console Errors

**Marketplace-attributable JavaScript runtime errors: ZERO** across all marketplace surfaces and dialogs.

Non-marketplace items observed (root-caused against built server chunks — NOT v4.3 defects):

| Item | Nature |
|---|---|
| `Settlement requires a SIGNED sale contract…` | By-design server-action validation rejection; surfaced to the user as a friendly inline message (the flow asserts it) |
| `Error: User has no organization` (system user) | Pre-existing shared dashboard org-name action (`getTenantSessionOrThrow → organization.findUnique`) that throws for the org-less SYSTEM user on ANY dashboard page. No marketplace file calls `getTenantSessionOrThrow`; all marketplace pages are client-only; admin page is `requireSystem`-guarded. Out of scope — flagged for a separate fix. |

---

## Mobile Viewport (375×812)

| Check | Result |
|---|---|
| /dashboard/marketplace renders | ✓ |
| No horizontal overflow | ✓ |
| No sub-30px tap targets | ✓ |
| Buyer-browse own-org exclusion (seller sees EmptyState, no cards) | ✓ (correct behavior) |

---

## axe-core Accessible-Name Scan

Rules: button-name, link-name, input-button-name, aria-command-name, aria-input-field-name, aria-toggle-field-name, select-name, label, image-alt, aria-tooltip-name.

- **Zero violations** on: marketplace browse, my-listings, detail, admin moderation, AND the publish / interest / edit / suspend dialogs, AND the modified pages (units, crm, dashboard).
- Pre-existing name violations on `/dashboard/settings`, `/dashboard/reports`, `/dashboard/maintenance/tickets` (unlabeled `<select>` / date inputs) — pre-date this feature, out of scope, tracked separately.

---

## Build / Types

- `prisma validate` ✓ · client regenerated ✓ (post-merge schema incl. main's v4.2.5 off-plan removal)
- `tsc --noEmit` ✓ green (merged tree)
- `next build` ✓ "Compiled successfully" — all four marketplace routes compiled

---

## Verdict

**PASS**

- Full cross-org workflow functional and verified end-to-end in a real browser
- Settlement gate (primary risk control) correctly blocks transfer without a signed contract
- Cross-org tenant isolation holds (curated read layer; own-org exclusion confirmed)
- Zero marketplace-attributable console errors; zero accessible-name violations on marketplace surfaces
- RTL/LTR + light/dark correct on all routes; mobile clean
- Branch synced to main @ v4.2.5; tsc + build green

Open items (pre-existing, NOT v4.3.0 regressions — track separately):
1. `Error: User has no organization` 500 for org-less SYSTEM users on dashboard shell (shared org-name action — needs a null-org guard).
2. axe accessible-name violations on `/dashboard/settings`, `/dashboard/reports`, `/dashboard/maintenance/tickets`.
