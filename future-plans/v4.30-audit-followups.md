# v4.30 Completeness-Audit Follow-ups (tracked)

> **Produced 2026-06-16** by an exhaustive multi-agent re-verification of the four audit/backlog
> sources (`QA-AUDIT-REMEDIATION.md`, `REMAINING-WORK.md`, `CX-REMEDIATION-HANDOVER.md`, the
> `verification/Mimaric-CX-Audit*.html`) against the **actual code + git history** at `main @ 5c7d03c`
> (v4.30.0), with an adversarial completeness critic. This file exists so the genuinely-incomplete
> items below are **tracked**, not silently dropped (the critic's key process finding: §3.3 + QA-DB-07
> had no deferral rationale logged anywhere).

## Audit verdict
- **CX-Audit (22 findings): 22/22 addressed** (21 done + CX-003 DB-region legitimately deferred). 0 missed.
- **CX-Handover (31): 26 done + 5 deferred.** 0 missed (one low SelectField-sweep tail).
- **QA-Audit (30): 19 done · 2 deferred · 3 missed · 6 partial.**
- **Remaining-Work (58): 36 done · 8 deferred · 3 missed · 10 partial · 1 obsolete.**
- **No falsely-marked-done defects** — marketplace P3 (5-gate settlement + dual kill-switch + CAS), the
  auth token flows (reset + email-verify both hash-at-rest + atomic single-use), and the Postgres-backed
  rate limiter were re-verified genuinely robust.

## Already fixed in v4.30.1 (this cycle)
- ✅ **PasswordResetToken hash-at-rest + atomic single-use** (was plaintext-at-rest + non-atomic consume) — the P0-class gap **no audit doc captured**. Shipped.
- ✅ **UnitsView bulk-status `<select>` aria-label** (QA-FE-03 tail — a real unguarded a11y violation axe couldn't see). Shipped.
- ⏳ **QA-TEST-01 tail** — org-isolation + coupon-concurrency regression tests (in progress this cycle).

## Genuinely missed — open (beyond the 4 known deferrals)

### MEDIUM
| ID | Item | Why it matters | Recommendation |
|---|---|---|---|
| **§3.3** | Ciphertext **envelope (versioned prefix) + DB CHECK** constraint on encrypted columns | `encryption.ts` still emits bare `iv:tag:ct` with no version byte; the write-time guard that would have caught the P1-1 plaintext leak. **Not** one of the 4 known deferrals — was silently dropped. | Next PII-touching schema change: add `v1:` prefix in `encrypt()`, branch `decrypt()`/`safeDecryptField` on it, backfill existing ciphertext, then add a Postgres `CHECK (col LIKE 'v1:%')` via `packages/db/sql`. |
| **decrypt() blind-spot** | `decrypt()` legacy-plaintext passthrough returns any non-`iv:tag:ct` value AS-IS with **no telemetry** | A plaintext-PII write (the P1-1 class) is invisible at READ time — there's no runtime signal. Reinforces §3.3. | Add a security-event log when `decrypt()`/`safeDecryptField` hits the legacy-plaintext branch on a column that should be encrypted, until the §3.3 envelope+CHECK lands. |
| **QA-FE-04** | CRM **CustomerDrawer** still hand-rolled (`fixed inset-0` + hardcoded `slide-in-from-right`), not `ResponsiveDialog` | The slide direction doesn't flip for Arabic RTL and there's no mobile bottom-sheet — the exact maintenance/RTL hazard the finding flagged. | Re-platform onto `ResponsiveDialog` (or a shared Drawer primitive) so RTL slide-from-end + mobile bottom-sheet are inherited. Verify in AR-RTL + 375×812 per §3.9. |

### LOW
| ID | Item | Recommendation |
|---|---|---|
| **QA-DB-07** | No retention/partition for `AuditLog`/`ConsentLog`/`Notification`/`WebhookEvent` (unbounded growth **+ PDPL data-minimization** for ConsentLog/AuditLog) — silently dropped | Scheduled purge (cron route or Supabase `pg_cron`) retaining AuditLog/ConsentLog per a **documented PDPL window**; prune delivered Notification/WebhookEvent older than N days. |
| **QA-ARCH-02** | Shared seams (`action-result` 0 importers, `ROUTES` 2 vs ~23 raw `revalidatePath` strings, `serialize` 2 vs ~24 inline, `t()` facade) still bypassed + no lint-bans | Add the prescribed lint-bans (no inline `JSON.parse(JSON.stringify)` in `app/actions`; no raw `revalidatePath("/dashboard…")` → force `ROUTES`); migrate the `revalidatePath` strings first (the live §8.5 stale-rename hazard). |
| **QA-ARCH-04** | `any`/exhaustive-deps budget — `massUpdateUnits` types `status?: any` (units.ts:87); 11 exhaustive-deps disables un-audited | Type `status` as the `UnitStatus` enum; focused pass on named `any`s; fix/justify each exhaustive-deps disable. Fold into the next touch of each file. |
| **QA-ARCH-03** | Dead **`apps/portal`** (3 redirect-only routes + stale "Create Next App" metadata title) not deleted/documented | Delete it (the USER-role `/portal` redirect resolves to `apps/web/app/portal`, NOT `apps/portal` — confirmed redundant) and drop from the build matrix, or add a top-of-file redirect-only comment. |
| **QA-MKT-06** | `viewCount` no per-viewer/session dedup (`listing-view.ts:201` increments on every view) | Gate on a `(listingId, viewerKey, date-bucket)` row or a short-TTL rate-limit key before incrementing; keep best-effort. (Search/pagination half is covered by CX-002.) |

### Risk-acceptance to confirm (gap no doc captured)
- **Rate limiter fails OPEN on DB error** (`rate-limit.ts`, deliberate availability choice) — a Postgres blip silently disables ALL throttling (login / password-reset / marketplace inquiry) at the worst moment. Consider a **fail-CLOSED** option for the most sensitive keys (login / password-reset), or document an explicit risk acceptance.

## Confirmed legitimate deferrals (Omar aware — not missed)
ZATCA e-invoicing · DB region move Sydney→Bahrain · full `t()` migration tail (`t-migration-deferred.md`) · true HTTP-403 edge middleware (CVE-2025-29927) · Ejar/off-plan/national-e-sign roadmap.

## PARTIAL (intentional staged rollout — not blocking)
- **QA-DB-04/05** NOT-NULL + FK tightening on `organizationId`/`*Hash` — gated on the prod backfill (`2026-06-orgid-backfill.sql`), owed by Omar (runbook `packages/db/sql/2026-06-v4.28-manual-steps.md`).
- **§3.6** `/dashboard/more` decommission — hub deleted; `more/profile` deliberately kept (UI-First linked).
- **§6.2** date-picker — react-aria migration done; full Jolly* component-stack spec-completeness is the tail.
